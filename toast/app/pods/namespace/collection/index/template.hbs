<section class="content-header">
  <h1>{{model.name}}</h1>
  <button class="btn btn-default" {{action "nextPage"}}>Next</button>
</section>

<section class="content">
  <div class="row">
    <div class="col-md-12">
      {{#if jsTreeData.isPending}}
        <h1>Loading...</h1>
      {{else}}
      {{#ember-fancytree data=jsTreeData.data}}
            <!-- <colgroup> -->
            <!--   <col width="30px"> -->
            <!--   <col width="30px"> -->
            <!--   <col width="*"> -->
            <!--   <col width="50px"> -->
            <!--   <col width="30px"> -->
            <!-- </colgroup> -->
            <thead>
              <tr>
                <th>Key</th>
                <th>Value</th>
                <th>Type</th>
              </tr>
            </thead>
            <tbody>
            </tbody>
      {{/ember-fancytree}}
      {{/if}}
    </div>
  </div>
</section>

{{outlet}}
