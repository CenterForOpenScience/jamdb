DEBUG: false

FORK: false
PORT: 50325
HOST: localhost
SENTRY_DSN: null

NAMESPACE_BACKENDS:
    state: ephemeral
    logger: ephemeral
    storage: ephemeral

COLLECTION_BACKENDS:
    state: ephemeral
    logger: ephemeral
    storage: ephemeral

NAMESPACEMANAGER_BACKENDS:
    state: ephemeral
    logger: ephemeral
    storage: ephemeral

JWT_SECRET: TestKey

OSF:
  URL: https://osf.io
  API_URL: https://api.osf.io
  ACCOUNTS_URL: https://accounts.osf.io
