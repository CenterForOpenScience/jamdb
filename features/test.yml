DEBUG: false

FORK: false
PORT: 50325
HOST: localhost

NAMESPACE_BACKENDS:
    state: ephemeral
    logger: ephemeral
    storage: ephemeral

NAMESPACE_DEFAULT_BACKENDS:
    state: ephemeral
    logger: ephemeral
    storage: ephemeral

NAMESPACEMANAGER_BACKENDS:
    state: ephemeral
    logger: ephemeral
    storage: ephemeral

JWT_SECRET: TestKey
